const GenreBadge = ({ label }) => (
  <span className="px-1.5 sm:px-2 py-0.5 rounded-full bg-surface-700 text-gray-400
                    text-[9px] sm:text-[10px] font-medium uppercase tracking-wide">
    {label}
  </span>
)

export default GenreBadge
